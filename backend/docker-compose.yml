services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: marketsphere
      POSTGRES_PASSWORD: marketsphere
      POSTGRES_DB: marketsphere
    ports:
      - "5432:5432"
    volumes:
      - marketsphere_pgdata:/var/lib/postgresql/data

volumes:
  marketsphere_pgdata:
